export function PoweredByCloudSync({ className = "" }: { className?: string }) {
  return (
    <p className={`text-[11px] font-medium tracking-wide text-zenith-muted ${className}`}>
      Powered by CLOUD SYNC Inc.
    </p>
  );
}
